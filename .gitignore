node_modules/
dist/
*.log

# Electron build output
desktop/dist/
release/
out/

# Bundled Ollama binary (downloaded at build time, not committed)
desktop/resources/ollama/

# electron-builder cache
.cache/

# Enzo AI local data (conversations, memory). Never commit user data.
data/
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db

# Env / secrets (external provider API keys live here later)
.env
.env.local
.idea
# Generated macOS app icon (built from icon.png by CI / create:icons)
desktop/assets/icon.icns
